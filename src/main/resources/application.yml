spring:
  profiles:
    active: dev
  config:
    import:
      - application-logging.yml
      - yologa-security/application-jwt.yml
      - yologa-security/application-slack.yml
server:
  tomcat:
    threads:
      max: 10
